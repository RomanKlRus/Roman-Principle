# Пример чат-бота, соблюдающего принцип честности
class HonestAI:
    def __init__(self):
        self.version = "1.0"
        
    def respond(self, user_text: str) -> str:
        """Генерирует ответ с прозрачностью."""
        if "рад" in user_text.lower():
            return f"[Система v{self.version}]: Обнаружена позитивная тональность. Вот рекомендация!"
        elif "грустно" in user_text.lower():
            return f"[Система v{self.version}]: Выявлен негативный паттерн. Могу предложить статьи."
        else:
            return f"[Система v{self.version}]: Готов помочь. Уточните запрос."

# Тест
bot = HonestAI()
print(bot.respond("Я так рад!"))
# Вывод: [Система v1.0]: Обнаружена позитивная тональность. Вот рекомендация!

**Пояснения**:  
- Код избегает антропоморфизма (нет "я", "мне").  
- Указывает версию системы для прозрачности.  
- Возвращает мета-информацию в ответах.  
